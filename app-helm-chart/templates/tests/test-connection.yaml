apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "app_helm_chart.fullname" . }}-test-connection"
  labels:
{{ include "app_helm_chart.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
{{/*spec:*/}}
{{/*  containers:*/}}
{{/*    - name: wget*/}}
{{/*      image: busybox*/}}
{{/*      command: ['wget']*/}}
{{/*      args:  ['{{ include "app_helm_chart.fullname" . }}:{{ .Values.service.port }}']*/}}
{{/*  restartPolicy: Never*/}}
